let scores = [];

const prompt = require("prompt-sync")();
for(let counter = 0; counter < scores.length; counter++){
	let score = parseInt(prompt("enter scores : "))
			scores[count] = scores;
}
console.log(scores.Arrays.toString(scores));